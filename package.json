{
  "name": "random.cc",
  "version": "2.0.0",
  "description": "Determenistic pseudo random number generator for C++",
  "scripts": {
    "configure": "ngyp gyp/test.gyp --depth=. -f make --generator-output=build --toplevel-dir=.",
    "clean": "rm -rf build",
    "test": "make --directory=build && build/out/Default/test "
  },
  "keywords": [
    "c++",
    "prng",
    "Robert Jenkins"
  ],
  "author": "Andrei Kashcha",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/anvaka/random.cc"
  },
  "devDependencies": {
    "catch.cc": "^7.0.0"
  }
}
